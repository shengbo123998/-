多国家多地区cvr精排模型优化，针对多国家多场景的模型偏差问题，加入动态权重强消偏提高模型预估准度。针对直播业务场景，加入更多直播间和用户的实时序列特征，以及统计特征，提高模型auc。线上AB实验在所有国家订单涨2.1%，时长0.18%，已推全实验。

- 哈希交叉的特征，商品池emb跟用户点击emb concat，输入哈希函数，生成新的sparse, 由于主播id不是很多(a)，用户交互过的更少（b）,最后对于每个主播id，生成的特征是（a*b）个emd也不多。


-优化方向： 1.采用focal loss，对难分类样本给予更高的权重，解决样本不平衡问题，或者解决UAUC低问题，也可以使用PPNET解决。
          poco:
                    1. 对特征进行分国家分场景建模，从特征层面上消偏。（美团Hinet, 快手pepnet）
                    2. 接入PPNET,输入国家和场景特征，进行场景个性化建模。
          


印尼地区sliding场景保量逻辑下线（时长模型和cvr模型各保量前五名），简化推荐系统，方便精排模型迭代。通过遗传算法的融合参搜索，线上AB实验场景内订单涨2.66%, 已推全实验。
- https://zhuanlan.zhihu.com/p/441117034 搜参



时长精排模型的内外流场景建模，针对内外流场景的用户观看时长分布不一致的问题，加入PN和star FCN进行多场景建模，离线AUC指标涨0.13%。

： 为什么要加入PN, 因为内外流场景的数据分布不一致，如果一起做bn的话，会造成某些场景的数据偏在一侧。不利于收敛

- Batch Normalization：对于一个mini-batch中的每个特征，在整个batch中计算该特征的均值和方差，然后利用这些均值和方差对每个样本进行归一化。
  Layer Normalization：对于每个样本，计算该样本所有特征的均值和方差，然后对该样本进行归一化。
